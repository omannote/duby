# CLAUDE.md

## Project Overview

**Duby** is an early-stage PHP project. The repository is currently minimal and under initial development.

## Repository Structure

```
duby/
├── README.md    # Project readme
├── test.php     # PHP entry point (placeholder)
└── CLAUDE.md    # This file
```

## Language & Runtime

- **Language:** PHP
- No framework, build system, or dependency manager (e.g., Composer) is configured yet.

## Development Workflow

### Branches

- `master` — main branch

### Getting Started

No build or install steps are required at this time. The project does not yet have dependencies or a configuration file (e.g., `composer.json`).

### Running Tests

No test framework is configured yet. When one is added, document the command here.

### Linting / Static Analysis

No linter or static analysis tool is configured yet.

## Conventions

- Keep commits small and descriptive.
- Follow PSR-12 coding style for PHP files when the project grows.

## Notes for AI Assistants

- This repository is in its earliest stages. Most infrastructure (CI, tests, linting, dependencies) has not been set up yet.
- When adding new files, prefer flat structure until complexity warrants subdirectories.
- Do not introduce frameworks or dependencies without explicit user approval.
- If adding PHP code, use `<?php` with strict types (`declare(strict_types=1);`) at the top of files.
