# بصمات نتائج مدروسة وثبت أنها ليست أسرارًا.
#
# الماسح يفحص التاريخ لا الشجرة الحالية، فتصحيح الملف في HEAD لا يمحو النتيجة
# من الالتزام الذي حملها. البصمة تستثني ذلك السطر وحده في ذلك الالتزام وحده —
# لا قاعدة مُعطَّلة ولا مسار مستثنى.
#
# UUID مكتوب حرفيًا في اختبار وحدة باسم `token`. رمز QR اعتماد حقيقي في هذا
# النظام، فالقاعدة محقّة في الاشتباه. صُحِّح المصدر ليولّد الرمز بدل كتابته
# (packages/shared/src/__tests__/schemas.test.ts)، وهذه البصمة للتاريخ فقط.
597d8d790cbc353623a0a364ecbe3b670c1c864b:packages/shared/src/__tests__/schemas.test.ts:generic-api-key:24
