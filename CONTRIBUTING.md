# التطوير على نظام دوبي

## التشغيل المحلي

الهدف: **مطور جديد يشغّل النظام كاملًا بخمسة أوامر**، بلا وصول إلى الإنتاج وبلا
مفاتيح حقيقية.

```bash
git clone https://github.com/omannote/duby.git && cd duby
pnpm install
cp .env.example .env.local
supabase start          # PostgreSQL + Auth + Storage محليًا
pnpm dev                # التطبيقان معًا
```

| العنوان                | التطبيق         |
| ---------------------- | --------------- |
| http://127.0.0.1:5173  | لوحة الموظفين   |
| http://127.0.0.1:5174  | صفحة العميل     |
| http://127.0.0.1:54323 | Supabase Studio |

بعد `supabase start` انسخ `anon key` من مخرجاته إلى `VITE_SUPABASE_ANON_KEY`
في `.env.local`.

## الأوامر

| الأمر                 | الغرض                                   |
| --------------------- | --------------------------------------- |
| `pnpm dev`            | التطبيقان في وضع التطوير                |
| `pnpm check`          | كل بوابات الجودة السريعة دفعة واحدة     |
| `pnpm typecheck`      | فحص الأنواع                             |
| `pnpm lint`           | ESLint                                  |
| `pnpm format`         | Prettier                                |
| `pnpm test:unit`      | اختبارات الوحدة                         |
| `pnpm test:db`        | اختبارات pgTAP (تتطلب `supabase start`) |
| `pnpm test:e2e`       | اختبارات المتصفح                        |
| `pnpm verify:headers` | فحص ترويسات الأمن في المستودع           |
| `pnpm gen:types`      | توليد أنواع TypeScript من المخطط        |
| `pnpm db:reset`       | إعادة بناء القاعدة من الهجرات           |

### بلا Docker؟ قاعدة محلية بديلة

يتطلب `supabase start` وجود Docker. إن لم يتوفر، يمكن التحقق من الهجرات
والاختبارات على PostgreSQL عادي:

```bash
# PostgreSQL 16 + pgTAP مطلوبان
./scripts/local-db.sh                    # bootstrap + كل الهجرات
SUPABASE_DB_URL="postgresql://postgres@/duby_test?host=/tmp&port=54399" \
  node scripts/run-db-tests.mjs
```

`tests/db/bootstrap-local.sql` ينشئ ما توفّره Supabase عادةً: الأدوار،
و`auth.users`، و`auth.uid()`، ومخطط التخزين. لا يُطبَّق على staging ولا الإنتاج.

### ملاحظة على اختبارات المتصفح

إن كان Chromium مثبتًا مسبقًا بإصدار مختلف عمّا تتوقعه الحزمة:

```bash
PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium-*/chrome-linux/chrome pnpm test:e2e
```

## قواعد لا يُدمج تغيير يخالفها

هذه ليست تفضيلات أسلوبية — كل واحدة تقابل عطلًا وقع فعلًا في النظام السابق.
الشرح الكامل في [`docs/system-analysis.md`](docs/system-analysis.md) القسم 8.

1. **لا سر في المستودع ولا في حزمة الواجهة.** `anon key` وحده يصل المتصفح،
   وأمانه من RLS. كل ما عداه في أسرار Edge Functions.

2. **كل قاعدة عمل حرجة تُفرض في PostgreSQL**، لا في JavaScript. وكل قيد يرافقه
   اختبار pgTAP في نفس الالتزام — قيد بلا اختبار لا يُدمج.

3. **العمليات الميدانية ذرّية.** المطابقة والتحقق وحفظ الصورة وتغيير الحالة
   في دالة واحدة: تنجح كلها أو لا أثر لها.

4. **الدوال تُستدعى بجلسة الموظف، لا بـ `service_role`.** الأخيرة لا تحمل
   `auth.uid()` فترد كل دالة `forbidden`. الاستدعاء بجلسة الموظف يجعل هويته
   مرتبطة بالتوقيع فلا تُنتحل، و`SECURITY DEFINER` تتيح الكتابة رغم أن RLS
   لا يمنح الواجهة أي صلاحية كتابة مباشرة.

5. **لا `refreshSession()` في معالج حدث.** يطلق حدث تغيير جلسة فيعيد تركيب
   الشجرة ويغلق النافذة قبل إرسال الطلب — هذا كان السبب الجذري لعطل تأكيد
   التسليم. قاعدة ESLint تمنعه.

6. **لا رسالة خطأ بلا رمز.** كل خطأ يعرض رمزًا من `@duby/shared/errors`
   ومعرّف الطلب. «تعذر إتمام العملية» وحدها ممنوعة.

7. **الترويسات تُراجَع كأي كود.** `_headers` في المستودع، ويفحصها CI قبل النشر
   وبعده. Cloudflare وحده يفسّرها، فلا يُكشف الخطأ فيها إلا بعد نشره.

8. **Service Worker لا يعترض Supabase إطلاقًا.** اعتراضه يخزّن بيانات عملاء في
   كاش المتصفح ويقدّم نسخة قديمة أثناء التشخيص.

9. **أي عطل إنتاج يُصلَح باختبار يفشل أولًا**، ثم إصلاح يجعله يمر.

## الهجرات

- للأمام فقط. التراجع هجرة جديدة.
- مرقّمة: `0042_add_delivery_photo_constraint.sql`.
- قابلة لإعادة التشغيل (`if not exists`, `create or replace`).
- تمر على `staging` قبل الإنتاج، بلا استثناء.
- تغيير كاسر ← نمط توسعي على مراحل، لا استبدال مباشر.

## سير العمل

```
فرع من main  →  PR  →  CI أخضر  →  دمج  →  نشر تلقائي إلى staging
                                              ↓
                              وسم vX.Y.Z  →  موافقة يدوية  →  الإنتاج
```

لا يُدمج إلى `main` قبل اجتياز: الأنواع، Lint، التنسيق، الترويسات، اختبارات
الوحدة، pgTAP، المتصفح، حجم الحزمة، فحص الأسرار، وتزامن الأنواع مع المخطط.
